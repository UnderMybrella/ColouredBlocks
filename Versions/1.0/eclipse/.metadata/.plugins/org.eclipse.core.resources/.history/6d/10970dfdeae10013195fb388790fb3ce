package org.abimon.mods.minecraft.deco;

import net.minecraft.init.Blocks;
import net.minecraft.init.Items;
import net.minecraft.inventory.InventoryCrafting;
import net.minecraft.item.Item;
import net.minecraft.item.ItemStack;
import net.minecraft.item.crafting.IRecipe;
import net.minecraft.world.World;

public class DecoRecipes implements IRecipe {

	public static final Item[][] STRUCTURE_BLOCK = new Item[][];
	
	@Override
	//
	public boolean matches(InventoryCrafting var1, World var2) {
		if(var1.getSizeInventory() == 9)
		{
			Item[][] items = new Item[3][3];
			for(int x = 0; x < 3; x++)
				for(int y = 0; y < 3; y++)
				{
					ItemStack itm = var1.getStackInRowAndColumn(x, y);
					if(itm != null)
						items[x][y] = itm.getItem();
					else
						items[x][y] = null;
				}
			
			if(itemEquals(items[0][1], Items.brick))
			
			return false;
		}
		else
			return false;
	}
	
	public boolean recipeEquals(Item[] items, Item[] recipe)
	{
		
	}
	
	public boolean itemEquals(Item one, Item two)
	{
		if(one == null || two == null)
			return false;
		if(one.getUnlocalizedName().equals(two.getUnlocalizedName()))
			return true;
		return false;
	}

	@Override
	public ItemStack getCraftingResult(InventoryCrafting var1) {
		return new ItemStack(Blocks.diamond_block);
	}

	@Override
	public int getRecipeSize() {
		return 9;
	}

	@Override
	public ItemStack getRecipeOutput() {
		return null;
	}

}
