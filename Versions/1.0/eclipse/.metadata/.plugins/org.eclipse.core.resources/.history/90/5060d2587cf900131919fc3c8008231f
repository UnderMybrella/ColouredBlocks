package org.abimon.mods.minecraft.colouredBlocks;

import net.minecraft.world.World;

public class UpdateThread extends Thread
{
	World w;
	int x, y, z;
	int seconds;
	
	public UpdateThread(World w, int x, int y, int z, int seconds)
	{
		this.w = w;
		this.x = x;
		this.y = y;
		this.z = z;
		this.seconds = seconds;
	}
	
	public void run()
	{
		while(true)
		{
			try
			{
				Thread.sleep(1000);
			}
			catch(Throwable th){}
		}
	}
}
